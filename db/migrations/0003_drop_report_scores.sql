-- レポートに「リラックス度の平均」「集中度の平均」を載せる設定を外す。
--
-- 測定値を平均した数値は載せない方針のため、この設定そのものが不要になった。
-- 平均は1回ごとの推移をならしてしまい、元の測定が表していたものが消える。
--
-- 0001 を適用済みでも未適用でも、この順に流せば同じ形になる。

ALTER TABLE salon_settings DROP COLUMN report_includes_scores;
